terraform for deploying wordpress in aws
to deploy set up variables in `terraform.tfvars` and run 
```
terraform init
terraform plan
terraform apply -auto-approve
```
After go to one of instances to setup wordpress and voila ;)